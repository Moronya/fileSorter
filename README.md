# fileSorter
  The program lets one sort their files effectively by specfying the directory path
# requirements:
  *  Python installed and the shutil module

  *  Ensure you specify the correct path

All elements will be grouped in their own folder based on the file extensions
